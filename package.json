{
  "name": "rust-auth-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Repository tooling (commit linting and git hooks) for the rust-auth workspace. Not published; the publishable npm package lives in packages/rust-auth.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.1.0",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.6"
  }
}
