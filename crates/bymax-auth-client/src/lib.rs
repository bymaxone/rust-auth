//! Native Rust HTTP auth client for `bymax-auth`, built on `reqwest`. It is the
//! typed, single-flight-refresh counterpart of the TypeScript `./client` for
//! service-to-service Rust consumers calling a `bymax-auth-axum` backend.
#![forbid(unsafe_code)]
#![deny(missing_docs)]
